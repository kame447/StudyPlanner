# SOLID file-by-file loop log

Status: active
Updated: 2026-08-14
Branch: `agent/browser-regression-audited-integration`
PR: #129

Parent inventory: `20260814-solid-file-by-file-refactor-and-md-inventory.md`
Roadmap: `../strategy/20260814-solid-refactor-roadmap.md`

This file is the compact per-loop execution log. The parent inventory keeps architecture rules, MD inventory and remaining-problem classification; this file records every new code loop so each iteration can update documentation without rewriting the large parent document.

| Loop | Primary file | Result | Verification | Status |
|---|---|---|---|---|
| 13 | `src/components/AppSettingsDialog.tsx` | support-only FAQ/contact/legal/version UI extracted to `AppSettingsSupportPanel.tsx`. | Existing Stable V5 fixed-mode contract preserved; support JSX moved without content change. | done |
| 14 | `src/components/ActualEditorCard.tsx` | plan/actual→draft, legacy alignment inference and relink candidate projection extracted to `src/lib/actualDrafts.ts`. | Added `src/lib/actualDrafts.test.ts`. | done |
| 15 | `src/components/ActualTrackingTools.tsx` | pure elapsed/timer/range calculations extracted to `src/lib/actualTracking.ts`; measurement UI/state kept cohesive. | Added `src/lib/actualTracking.test.ts`. | done |
| 16 | `src/components/AdminApp.tsx` | no change; small admin composition root is acceptably cohesive. | Auth/navigation responsibilities are local to the admin shell and no reusable abstraction was found. | done |
| 17 | `src/components/AdminGuard.tsx` | no change; guard only maps `checking / denied / allowed` status to access-boundary rendering. | Authorization decision remains owned by `useAdminStatus`; props expose only status and children. | done |
| 18 | `src/components/AdminViews.tsx` | extracted report formatting, report panel and report-list presentation into `AdminReportViews.tsx`; retained route and user data-loading ownership in `AdminViews`. | Added `AdminReportViews.test.tsx` for mode labels, signed deltas and shared metric rendering. | done |
| 19 | `src/components/AiRuntimeSettings.tsx` | no change; local AI-config editing UI is cohesive and provider/proxy/validation/storage semantics are already delegated to `src/lib/aiConfig.ts`. | Props are limited to `config / onSave / onReset`; no duplicated policy or provider implementation detail found in the component. | done |
| 20 | `src/components/AuthScreen.tsx` | extracted limited-public access-key input/validation/unlock lifecycle into `AuthAccessGateForm.tsx`; normal sign-in/sign-up surface stays in `AuthScreen`. | Added `AuthAccessGateForm.test.tsx` covering failed and successful unlock paths with state updates sequenced explicitly. | done |
| 21 | `src/components/BookshelfView.tsx` | extracted current-user active-material selection, missing-subject fallback reconstruction, and subject grouping/sorting into `src/lib/bookshelfMaterials.ts`. | Added `src/lib/bookshelfMaterials.test.ts` covering ownership/status filtering, fallback subject reconstruction and deterministic grouping order. | done |
| 22 | `src/components/DatePickerDialogs.tsx` | moved the independent day-calendar picker into `DayCalendarDialog.tsx`; `DatePickerDialogs` now owns week selection and re-exports the day picker for caller compatibility. | Existing day-picker behavior moved without semantic change; `MonthEventDialog` compatibility is preserved through the re-export. | done |
| 23 | `src/components/DayNotebookPanel.tsx` | removed dead component surface. | Repository search found no external `DayNotebookPanel` reference and current `DayView` does not mount it. | done |
| 24 | `src/components/DayTimeline.tsx` | removed duplicate actual title/subject/alignment inference by reusing `actualDrafts`; extracted overlap lane assignment into `src/lib/dayTimelineLayout.ts`. | Added `src/lib/dayTimelineLayout.test.ts` covering non-overlap, overlap, and minimum-visual-height grouping. | done |
| 25 | `src/components/DayView.tsx` | extracted independent day-material workflows into `DailyMaterialShelf.tsx` and `MaterialQuickCreateModal.tsx`; reused shared bookshelf grouping and quick-entry duration/end-time contracts. | Extraction reviewed against pre-refactor implementation; component regression gap later hardened in Loops 38-39. | done |
| 26 | `src/components/FloatingActualTrackingPanel.tsx` | no change; component is a cohesive draggable/collapsible shell around `ActualTrackingTools`. | Tracking calculations/state remain delegated to Loop 15 owner; position clamp is shell-specific with no reuse target. | done |
| 27 | `src/components/InitialPrivacyConsentScreen.tsx` | no change; component owns only consent-screen interaction state and delegates persistence/retry/sign-out policy through callbacks. | Privacy copy intentionally untouched because Issue #45 owns lifecycle/governance semantics. | done |
| 28 | `src/components/InitialWeekStartPreferenceScreen.tsx` | no change; first-run week-start selection UI owns only local selection/submitting/error state. | Week interpretation and persistence remain outside the component through typed `WeeklyPlanningWeekStartsOn` contract and callbacks. | done |
| 29 | `src/components/LegalPage.tsx` | no change; static legal copy and a small generic renderer remain together to avoid risky copy movement without architectural benefit. | Legal/privacy wording intentionally untouched. | done |
| 30 | `src/components/MonthEventDialog.tsx` | deferred refactor; save normalization, recurrence delete-scope policy, and editor UI are mixed. | Defer until focused/full tests can execute. | done |
| 31 | `src/components/MonthView.tsx` | deferred refactor; month pager gesture state, keyboard navigation, daily aggregation, and cell rendering are distinct responsibilities. | Defer pager split until browser regression coverage is available. | done |
| 32 | `src/components/MonthPickerDialog.tsx` | no change; DOM measurement, scroll-settle handling, and year/month selection form one cohesive wheel-picker interaction. | No business policy or reusable data-access dependency embedded. | done |
| 33 | `src/components/MyPageDialog.tsx` | no change; profile/avatar editing, account actions, and optional admin navigation remain within one account/profile modal responsibility. | Policy remains delegated to helpers/hooks/callbacks. | done |
| 34 | `src/components/PlanFieldsEditor.tsx` | no change; typed `PlanDraft` field editing is a single presentational responsibility. | Existing libraries/components own plan/repeat/time behavior. | done |
| 35 | `src/components/PlanEditorPanel.tsx` | no change; modal submit/cancel orchestration and minimal edit validation are cohesive. | Recurrence scope mutation is not owned here. | done |
| 36 | `src/components/TimeRangeFields.tsx` | no change; `TimeWheelPicker` and range wrapper are one time-input interaction unit. | Duration/auto-end calculations already live in `lib/date`. | done |
| 37 | `src/components/StandaloneActualEditorCard.tsx` | extracted standalone actual duration, same-day end-time, save-draft and link-candidate projections into `src/lib/standaloneActualDrafts.ts`. | Added `src/lib/standaloneActualDrafts.test.ts`; preserved 24:00 clamp and raw candidate scoring values. | done |
| 38 | `src/components/DailyMaterialShelf.tsx` | adversarial audit found a behavior regression: sorting grouped materials before choosing missing-subject fallback metadata could change displayed subject name/color. Reused `buildSubjectsWithMaterialFallback` so fallback identity is derived from source order before card sorting. | Added `DailyMaterialShelf.test.tsx` for conflicting legacy fallback metadata + sorted card order. | done |
| 39 | `src/components/MaterialQuickCreateModal.tsx` | seven-audit test-quality pass found the Loop 25 extraction had no direct component regression. No production behavior defect found; added component-level contract coverage instead of changing implementation. | Added `MaterialQuickCreateModal.test.tsx` covering default standalone-actual draft, cross-midnight clock behavior, and plan-tab draft mapping. | done |
| 40 | `src/components/ReportView.tsx` / `src/App.tsx` | seven-audit ISP pass found `dayNote`, `monthEvents`, and `onSaveDayNote` were mandatory ReportView props but explicitly discarded. Removed the dead props and caller-only `createEmptyDayNoteDraft` / `currentDayNote` / `saveDayNote` plumbing. | No report calculation or rendering branch changed; current-head typecheck/full tests/build/browser must confirm caller contract after this loop. | done |
| 41 | `.github/workflows/browser-regression.yml` | CI harness audit found the isolated runner pinned to Playwright 1.55.0 while its install reported two high-severity audit findings. Updated only the isolated e2e runner to official stable 1.62.1; application manifests remain checksum-protected. | Final Browser Regression must prove the same 80 contracts against the updated runner; production package.json/package-lock.json are not modified. | done |
| 42 | `docs/ai/tasks/` active queue | removed three root-level duplicates whose closed records already exist: current-time boundary, Stable V5 verification/cutover, and PR #120 legacy-concept audit pointer. | Closed copies were verified before deletion; `20260810...human-reviewed...` was not moved because no closed copy exists. | done |

Verification terminology: `Added ...test` means a regression test file was added. It does not mean the test executed successfully. Execution/pass status must come from PR #129 CI or an equivalent explicit test run.

Per-loop rule: before starting the next code file, update this log and `docs/ai/strategy/20260814-solid-refactor-roadmap.md`.
