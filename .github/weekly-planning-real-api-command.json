{
  "profile": "merge-gate",
  "repetitions": 3,
  "request_id": "20260819-pr157-merge-gate-22",
  "reason": "Real Luna 3x merge gate after gate 21. Gate 21 showed two remaining orchestration defects: valid 8-minute-per-unit alternate effort could fail in the focused contextual route under a tight completion-token cap, and a meaningful deadline completeness retry could be rejected because an existing study-task delta shell omitted durable study metadata. The focused contextual cap now has modest headroom without changing prompt semantics, and existingPublicId study shells are normalized only when verified against public state; transient unknown study metadata is removed by existing-entity rebase before commit. Deterministic CI and Browser Regression are green on the implementation head. Revalidate 13:00 deadline acceptance plus visible acknowledgement before resumed progress, bounded 20 -> completed 12 -> remaining 8 -> explicit 8 min/unit bound to remaining 8 with no repeated completed-total question, remaining 45-minute and two-hour targetFactId directionality, direct completed-duration observed pace, 100% completion and correction behavior, typed renderer/all question codes, transcripts, and Fact Graph invariants."
}
