{
  "profile": "merge-gate",
  "repetitions": 3,
  "request_id": "20260826-pr195-timebox-overasking-merge-gate-4",
  "reason": "Final repeated real-Luna merge gate for PR #195 after manual review of gate 3 exposed a real stochastic semantic duplication: one run emitted the same 1日1時間 workload at both task and 金フレ component scope under different localIds, leaving two active workload facts and producing 13 candidates for the 2026-08-27..2026-09-07 inclusive range instead of 12. The deterministic duplicate-placement normalizer now treats localId only as identity and removes a task-level copy only when exactly one component workload has a byte-equivalent typed/evidence payload; it still refuses fuzzy, conflicting, multi-component, or distinct-evidence cases. The real-API gate now requires exactly one active matching time workload and exactly 12 unsplit 60-minute candidates for the 金フレ request. Revalidate progress-overasking, optional spaced-memory proposal and rejection, existing-task deadline side contribution, completion/correction, remaining-effort binding, typed renderer, all question codes, reading, thesis timebox, weekday vocabulary, remaining-time scheduling, transcripts, and Fact Graph invariants."
}
