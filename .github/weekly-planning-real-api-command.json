{
  "profile": "merge-gate",
  "repetitions": 3,
  "request_id": "20260819-pr157-merge-gate-19",
  "reason": "Final Real Luna 3x merge gate after deterministic CI exposed that the side-contribution guard treated every semantic task without existingPublicId as independent new work. Short pending-question replies are allowed to arrive as temporary AI task shells, so that rule bypassed the exact contextual binder and regressed observed-pace, per-unit effort, memory calibration, atomic incompatible-reply handling, and ordinary short-duration turns. The routing boundary now classifies independent meaning from typed semantic payloads rather than task identity alone, preserves deadlines and genuine target-only new work through normal canonicalization, and limits the separate estimate target to the typed completed_workload_total question basis. Full deterministic CI is green on the repaired implementation. Revalidate final conversation, completed progress, remaining effort, typed renderer, all question codes, transcript, and Fact Graph invariants on this exact head."
}
