name: PR5 reasoning key fix

on:
  push:
    branches:
      - agent/weekly-planning-conversation-hardening

permissions:
  contents: write
  issues: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/weekly-planning-conversation-hardening
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Apply reasoning key fix
        id: fix
        continue-on-error: true
        run: |
          set -o pipefail
          python .github/scripts/pr5_fix_reasoning_key.py 2>&1 | tee /tmp/reasoning-key-fix.log

      - name: Commit reasoning key fix
        if: steps.fix.outcome == 'success'
        run: |
          if git diff --quiet; then
            echo 'No reasoning key changes required.'
            exit 0
          fi
          git diff --check
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/features/weeklyPlanning
          git commit -m "feat: 保存済みdraftのreasoning key契約を閉じる"
          git push origin HEAD:agent/weekly-planning-conversation-hardening

      - name: Report reasoning key result
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.existsSync('/tmp/reasoning-key-fix.log')
              ? fs.readFileSync('/tmp/reasoning-key-fix.log', 'utf8').slice(-12000)
              : 'reasoning-key log not found';
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 20,
              body: `reasoning-key outcome: ${{ steps.fix.outcome }}\nhead: ${context.sha}\n\n\`\`\`text\n${log}\n\`\`\``,
            });

      - name: Fail on reasoning key error
        if: steps.fix.outcome != 'success'
        run: exit 1
