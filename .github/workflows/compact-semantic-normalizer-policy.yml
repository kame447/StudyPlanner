name: Compact Semantic Normalizer Policy
on:
  push:
    branches: [agent/weekly-ai-conversation-eval]
    paths:
      - .github/compact-semantic-normalizer-policy.py
      - .github/workflows/compact-semantic-normalizer-policy.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {ref: agent/weekly-ai-conversation-eval, fetch-depth: 0}
      - run: |
          git fetch origin agent/weekly-ai-conversation-eval
          git reset --hard origin/agent/weekly-ai-conversation-eval
          python .github/compact-semantic-normalizer-policy.py
          git config user.name kame447
          git config user.email 76802107+kame447@users.noreply.github.com
          git add src/features/weeklyPlanning/semantic/weeklyPlanningSemanticNormalizerV5.ts
          git commit -m "refactor: compact semantic normalizer policy"
          git push origin HEAD:agent/weekly-ai-conversation-eval
