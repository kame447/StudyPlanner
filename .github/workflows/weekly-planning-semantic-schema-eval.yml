name: Weekly Planning Semantic Schema Eval

on:
  push:
    branches:
      - test/weekly-planning-semantic-schema-eval
  pull_request:
    branches:
      - main
    paths:
      - 'src/features/weeklyPlanning/semanticV2/**'
      - '.github/workflows/weekly-planning-semantic-schema-eval.yml'
  workflow_dispatch:

permissions:
  contents: read
  models: read

concurrency:
  group: weekly-planning-semantic-schema-eval-${{ github.ref }}
  cancel-in-progress: true

jobs:
  real-eval:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      WEEKLY_PLANNING_SEMANTIC_SCHEMA_V1_TARGETED_EVAL: '1'
      GITHUB_MODELS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run targeted generic semantic schema evaluation
        id: eval
        continue-on-error: true
        run: >-
          npm run test:run --
          src/features/weeklyPlanning/semanticV2/weeklyPlanningSemanticExperimentV1.targeted.real-eval.test.ts
      - name: Upload evaluation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: weekly-planning-semantic-schema-v1-targeted-eval
          path: artifacts/weekly-planning-semantic-schema-v1-targeted-eval.json
          if-no-files-found: warn
      - name: Propagate evaluation failure
        if: steps.eval.outcome == 'failure'
        run: exit 1
