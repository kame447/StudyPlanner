name: PR68 stage4 build debug

on:
  pull_request:
    branches: [main]
    paths: [.github/workflows/pr68-stage4-build-debug.yml]

permissions:
  contents: write

jobs:
  capture:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Capture build failure
        run: |
          git worktree add /tmp/pr68-stage4-build HEAD
          set +e
          cd /tmp/pr68-stage4-build
          python3 scripts/pr68_stage4_fix.py >/tmp/pr68-stage4-build-patch.txt 2>&1
          npm ci >/tmp/pr68-stage4-build-install.txt 2>&1
          npm run build >/tmp/pr68-stage4-build.txt 2>&1
          status=$?
          cd "$GITHUB_WORKSPACE"
          cp /tmp/pr68-stage4-build.txt pr68-stage4-build.txt
          echo "build_status=$status" >> pr68-stage4-build.txt
          rm .github/workflows/pr68-stage4-build-debug.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pr68-stage4-build.txt .github/workflows/pr68-stage4-build-debug.yml
          git commit -m 'feat: 院試scope修正のbuild診断を記録'
          git push origin HEAD:agent/fix-weekly-planning-trace-and-dialogue-final
