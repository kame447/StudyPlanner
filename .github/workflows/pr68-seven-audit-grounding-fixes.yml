name: PR68 seven audit grounding verification

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/pr68-seven-audit-grounding-fixes.yml

permissions:
  contents: write

jobs:
  verify:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply final grounding fixes
        run: |
          python3 scripts/pr68_seven_audit_grounding_fixes.py
          python3 scripts/pr68_seven_audit_grounding_escape_fix.py
      - name: Check diff
        run: git diff --check
      - name: Install dependencies
        run: npm ci
      - name: Run focused tests
        run: |
          npm run test:run -- \
            src/features/weeklyPlanning/__tests__/weeklyPlanningAdversarialInput.test.ts \
            src/features/weeklyPlanning/__tests__/weeklyPlanningSevenAuditContract.test.ts \
            src/features/weeklyPlanning/__tests__/weeklyPlanningInterpreterFoundation.test.ts \
            src/features/weeklyPlanning/intake/weeklyPlanningCandidateValidator.test.ts \
            src/features/weeklyPlanning/intake/weeklyPlanningQuestionSlots.test.ts \
            src/features/weeklyPlanning/dialogue/weeklyPlanningAiDialogueRenderer.test.ts
      - name: Run full tests
        run: npm run test:run
      - name: Build
        run: npm run build
      - name: Commit verified fixes and clean temporary files
        run: |
          rm -f scripts/pr68_seven_audit_grounding_fixes.py
          rm -f scripts/pr68_seven_audit_grounding_escape_fix.py
          rm -f .github/workflows/pr68-seven-audit-grounding-fixes.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'feat: 週間計画のgrounding境界を再整合'
          git push origin HEAD:agent/fix-weekly-planning-trace-and-dialogue-final
