// Deprecated compatibility shim.
// localStorage-based persistence now lives in localStorageGateway.ts so that
// repositories depend on gateway interfaces and can be replaced by external DB implementations later.
export {};
